# If you need to customize Behat, copy this to behat.yml and do it there
imports:
    - behat_defaults.yml

default:
    context:
        parameters:
            base_url: http://127.0.0.1:8401

web:
    extensions:
        Behat\MinkExtension\Extension:
            base_url: http://127.0.0.1:8401
